---
description: Produce a homeowner insight summary and GC email from invoice audit findings
---

Use the `invoice:email-drafter` skill to produce two outputs from invoice audit findings: (1) a homeowner insight summary highlighting what actually matters — trends, concerns, patterns — and (2) a plain-text email to the GC. Requires a prior audit run (extracted.json, matching_results.json, or XLSX report).
