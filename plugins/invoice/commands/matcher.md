---
description: Match parent invoice line items to supporting documents
---

Use the `invoice:matcher` skill to cross-reference and match line items against their supporting documents. Requires `extracted.json` from a prior `invoice:ingest` run. If no extracted.json exists, run `invoice:ingest` first.
